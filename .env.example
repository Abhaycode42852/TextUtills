# Copy this file to ".env" (same folder as docker-compose.yml) and fill
# in your own Docker Hub username. ".env" itself should NOT be committed
# if you ever put anything secret in it - for this project it's just a
# public username, but it's good habit to keep .env out of git regardless.

DOCKERHUB_USERNAME=yourusername
